import Pricing from '@/components/auth/Pricing';

export const metadata = {
  title: 'Тарифы — GAME',
  description: 'Free и Pro. Открой полный доступ к ядру Max.',
};

export default function PricingPage() {
  return <Pricing />;
}
