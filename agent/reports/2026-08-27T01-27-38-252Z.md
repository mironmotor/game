# Ночной обход 2026-08-27T01-27-38-252Z
Режим: рука ядра (ходов 19, $0.414)

Ничего не менял — только читал. Вот что вижу.

## Заявка h1787793907739: чем заменить камеру

**Почему research пустой — это не поломка.** `ultra_orchestrator.py:165` считает `web_access` только если стоят **оба** флага: `MAX17_WEB_ENABLED` **и** `MAX17_AUTO_WEB`. В `deploy/com.max17.game.plist:33` есть только первый. Значит любой `research` из оркестратора гарантированно вернёт ноль фактов, а `futility.py` (порог 3 пустых подряд) сработал ровно как задумано и выпихнул задачу руке. Смена подхода была правильной. *Догадка:* на сервере флаги те же, что в plist — проверить не могу, туда не лезу.

**Главная находка по существу заявки.** `environment.py:118-128`: при выключенной камере возвращается `presence: "away"` с уверенностью 0.3, и это уходит в память как метаданные (`json_cli.py:981`), а `responder.py:1111` вслух говорит «Похоже, тебя сейчас нет в кадре». То есть **отсутствие сенсора трактуется как отсутствие человека** — и накапливается в памяти как факт. Предложение: отдавать `presence: "unknown"` и не писать ассоциацию отсутствия, когда `active=false`. Одна правка в одной ветке.

**Замены камере, которые уже есть в репозитории и не требуют железа:**
- `voice_state.py` — просодия с микрофона через HUD. Речь = человек рядом; даёт ещё и состояние (устал/напряжён). В `json_cli.py:3862` `voice_observation` идёт по тому же тракту, что и `environment_observation` — канал уже сшит с памятью, новый модуль не нужен.
- `music_sense.py` — тот же микрофон, второй независимый признак «в комнате что-то происходит».
- `chrono.py` — фаза дня без сенсоров вообще, дешёвый приор присутствия.
- `heart.py` / `self_state.py` — проприоцепция: своё состояние как контекст, когда внешнего нет.
- `hands.py` — спросить человека. Самый дорогой канал, но самый точный.
- `desktop_agent.py` — `screencapture`/AppleScript, но это Tier-1 с подтверждением и только локальный мак; как фоновый сенсор не годится.

**Приборы.** Всё в норме: диск 14%, память 1.3 ГБ свободно, 4 процесса online, аптайм 5 суток. Сайт 1.1 с — заметно, но это одно измерение, тревогу не поднимаю.

## Пять пунктов по важности

1. `environment.py:118` — выключенная камера пишется в память как «человека нет». Чинить: `presence: "unknown"`, без ассоциации.
2. `MAX17_AUTO_WEB` не выставлен → весь автономный research мёртв by design. Решение за тобой: включить или окончательно отдать веб руке.
3. Голосовой канал недоиспользован как сенсор среды — тракт готов, логики «голос → присутствие» нет.
4. `responder.py:1111` озвучивает «тебя нет в кадре» на основании п.1 — человек слышит неправду.
5. Ответ сайта 1.1 с — присмотреться на следующем обходе, пока не проблема.

РУКА h1787793907739: камеру заменить нечем «в лоб», но голос (`voice_state.py`) уже идёт по тому же тракту памяти и даёт присутствие бесплатно — а сейчас выключенная камера ошибочно пишется в память как `presence: "away"` (`environment.py:118`), и это надо чинить раньше любых новых сенсоров.
