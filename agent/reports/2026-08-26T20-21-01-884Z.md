# Ночной обход 2026-08-26T20-21-01-884Z
Режим: рука ядра (ходов 20, $0.517)

Осмотр закончен. Ничего не правил — только чтение.

## Кадр `mt904eqk31vzskcu.png`

PNG 120×90, 258 байт, RGB. Три горизонтальные полосы равной высоты во всю ширину: **малиново-розовая** сверху, **бирюзово-зелёная** посередине, **жёлтая** снизу. Ровные заливки, границы чёткие, без градиентов, шума и сжатия.

**Текста нет вообще.** Нет объектов, лиц, фона, перспективы — ничего, кроме трёх цветов.

Что важно: это не фотография реальности, а тестовая картинка либо заглушка. 258 байт при 120×90 — размер, на котором никакая модель зрения не скажет ничего содержательного. `_read_image` (mark17/vision_core.py:335) ужимает только вниз, при `max(size) > 1024`, апскейла нет — в модель уйдёт ровно этот огрызок. Роут отдачи (app/api/media/[id]/route.ts) отдаёт файл как есть и ничего не портит, так что таким кадр и приехал. Если ждали настоящий снимок — отправка не дошла, стоит попросить человека прислать заново.

## Что требует внимания

Приборы чистые: диск 14%, память 1.3 ГБ свободно, четыре процесса online, сайт 200. `tsc --noEmit` проходит без ошибок. Проблемы — в свежей логике руки, написанной час назад (коммиты 290b4b5, 5e74cd7, 65a8439).

1. **`request()` не фильтрует отвеченные — очередь забивается мертвецами.** `mark17/hands.py:90`: `pending = [r for r in rows if not r.get("taken_at")]`. Коммит 5e74cd7 добавил фильтр `answered` в `pending()` и `take()`, но пропустил `request()`. После `reclaim()` закрытая по лимиту попыток заявка получает `taken_at = None` и навсегда остаётся в очереди, продолжая считаться ожидающей. Накопится 12 — и ядро получает «очередь полна, рука ещё не разобрала прошлое» на любую заявку. Это глушит агентность ровно тем способом, который чинили. *Предложение:* добавить в строку 90 такой же фильтр `str(r.get("id")) not in _answered_ids(state_dir)`.

2. **Падение после отчёта шлёт ядру ложный провал.** `agent/night.mjs:157` ставит `claimedTasks = coreTasks`, но после успешного цикла отчётов (241–248) список не очищается. Строка 256 — `warn()` в телеграм, обычный сетевой вызов, падает легко. Тогда `catch` на 263 отчитается провалом по уже успешно закрытым заявкам, и MAX запишет в память «рука не справилась» о сделанной работе. *Предложение:* `claimedTasks = []` после строки 248, либо вычёркивать задачу по мере отчёта.

3. **Обрезка результатов до 200 воскрешает старые заявки.** `report()` пишет `rows[-200:]`, а признак «отвечено» берётся из этого же файла (`_answered_ids`). Когда ответ вытесняется за границу, давняя взятая заявка снова выглядит неотвеченной, `reclaim()` вернёт её в очередь, и рука выполнит её повторно — для `kind:"do"` это повтор действия в реальности. Дубликаты из пункта 2 ускоряют вытеснение. *Догадка* в части частоты: зависит от темпа заявок, точно не проверить без живого state. *Предложение:* хранить закрытые id отдельным компактным набором, не обрезаемым вместе с текстами.

4. **Гонка при записи очереди.** `_write` атомарен на подмене файла, но `reclaim`/`take`/`request` делают read-modify-write без блокировки. Ядро на дроплете (через `stats`/`pending`) и агент на маке (через `take`) конкурируют за `hands_queue.jsonl`. Окно в миллисекунды, но `stats()` зовётся каждым тактом. Исход — только что взятая заявка вернётся в очередь и уйдёт руке дважды. *Догадка,* воспроизвести не пробовал. *Предложение:* `fcntl.flock` вокруг очереди.

5. **`reclaim()` пишет на диск из читающих функций.** `stats()` (hands.py:232) и `pending()` (:119) — геттеры для снимка состояния, теперь оба меняют очередь, а внутри ещё и вызывают `report()`. Это же и питает гонку из пункта 4. *Предложение:* звать `reclaim()` явно, одним местом в такте ядра.

РУКА h1787682699698: кадр — тестовая заглушка 120×90 в 258 байт, три полосы (розовая, бирюзовая, жёлтая), текста и объектов нет; смотреть не на что, стоит попросить прислать снимок заново.

## Куда не пустили
- Bash: не дал
