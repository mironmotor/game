"""Strategy-health dashboard (Stage 3 stub).

Plan: a small web dashboard (FastAPI + a static page, or Streamlit) showing
the live equity curve, open risk, regime, recent decisions, and scam-flag
status. Stage 1 ships a terminal summary instead (see ``print_console`` and
the generated markdown report).
"""

from __future__ import annotations


def print_console(metrics: dict) -> None:
    print("Dashboard (Stage 3) not yet built; see the markdown report in reports/output/.")
