# GAME MARKET CORE
#
# Stage 1 runs on the Python standard library ONLY. There are no required
# third-party packages — `python3 main.py` works out of the box. The
# packages below are OPTIONAL and unlock later stages; install per group.

# ---- Stage 2: real data, faster numerics, plotting ----
# numpy>=1.24
# pandas>=2.0
# pyyaml>=6.0          # nicer config parsing (a stdlib fallback parser ships in config.py)
# requests>=2.31       # REST data loaders (FRED, Stooq, exchange REST)
# matplotlib>=3.7      # equity-curve / drawdown charts

# ---- Stage 3: real-time + news + dashboard ----
# websockets>=12.0     # exchange websocket streams
# feedparser>=6.0      # RSS news loader
# fastapi>=0.110       # dashboard API
# uvicorn>=0.29        # dashboard server

# ---- Stage 4: ML layer ----
# scikit-learn>=1.3    # baseline models, regime classifier
# lightgbm>=4.0        # gradient boosting (preferred)
# xgboost>=2.0         # alternative gradient boosting
# Sequence models (LSTM/Transformer) only AFTER a baseline beats trivial:
# torch>=2.0
